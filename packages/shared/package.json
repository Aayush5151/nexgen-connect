{
  "name": "@nexgen-connect/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Platform-agnostic shared values for NexGen Connect: theme tokens, copy strings, validators, and types consumed by both web (Next.js) and mobile (Expo). No React, no Next, no Expo imports — pure TS so both surfaces can consume cleanly.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./theme": "./src/theme.ts",
    "./corridors": "./src/corridors.ts",
    "./constants": "./src/constants.ts"
  },
  "scripts": {
    "lint": "echo 'no lint configured for shared yet'",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
